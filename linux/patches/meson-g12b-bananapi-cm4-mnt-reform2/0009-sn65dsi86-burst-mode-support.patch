diff --git a/drivers/gpu/drm/bridge/ti-sn65dsi86.c b/drivers/gpu/drm/bridge/ti-sn65dsi86.c
index 7a748785c545..729961aec052 100644
--- a/drivers/gpu/drm/bridge/ti-sn65dsi86.c
+++ b/drivers/gpu/drm/bridge/ti-sn65dsi86.c
@@ -683,6 +683,10 @@ static int ti_sn_attach_host(struct ti_sn65dsi86 *pdata)
 	dsi->lanes = 4;
 	dsi->format = MIPI_DSI_FMT_RGB888;
 	dsi->mode_flags = MIPI_DSI_MODE_VIDEO;
+	if (of_property_read_bool(pdata->dev->of_node, "burst-mode")) {
+		dsi->mode_flags |= MIPI_DSI_MODE_VIDEO_BURST;
+		printk(KERN_ALERT "DEBUG ti_sn65dsi86: burst mode enabled\n");
+	};
 
 	/* check if continuous dsi clock is required or not */
 	pm_runtime_get_sync(dev);
