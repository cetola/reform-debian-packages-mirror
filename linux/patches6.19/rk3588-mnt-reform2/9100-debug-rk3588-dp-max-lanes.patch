commit 44a444f141428911a81e5bdf2b4543eaf846718e
Author: Lucie L. Hartmann <lukas@mntre.com>
Date:   Tue Apr 7 21:19:31 2026 +0200

    DEBUG: rk3588/eDP: experimental max_lanes/link_rate override

diff --git a/drivers/gpu/drm/bridge/analogix/analogix_dp_core.c b/drivers/gpu/drm/bridge/analogix/analogix_dp_core.c
index efe5349..5e4fb3a 100644
--- a/drivers/gpu/drm/bridge/analogix/analogix_dp_core.c
+++ b/drivers/gpu/drm/bridge/analogix/analogix_dp_core.c
@@ -559,6 +559,12 @@ static int analogix_dp_full_link_train(struct analogix_dp_device *dp,
 	/* Initialize by reading RX's DPCD */
 	analogix_dp_get_max_rx_bandwidth(dp, &dp->link_train.link_rate);
 	analogix_dp_get_max_rx_lane_count(dp, &dp->link_train.lane_count);
+	dev_err(dp->dev, "[analogix-DP-debug] link_rate: %x\n", dp->link_train.link_rate);
+	dev_err(dp->dev, "[analogix-DP-debug] lane_count: %x\n", dp->link_train.lane_count);
+	dev_err(dp->dev, "[analogix-DP-debug] max_rate: %x\n", max_rate);
+	dev_err(dp->dev, "[analogix-DP-debug] max_lanes: %x\n", max_lanes);
+	dp->link_train.lane_count = 2;
+	dev_err(dp->dev, "[analogix-DP-debug] lane_count override: %x\n", dp->link_train.lane_count);
 
 	if ((dp->link_train.link_rate != DP_LINK_BW_1_62) &&
 	    (dp->link_train.link_rate != DP_LINK_BW_2_7) &&
@@ -1403,12 +1409,6 @@ static int analogix_dp_dt_parse_pdata(struct analogix_dp_device *dp)
 	case RK3588_EDP:
 		video_info->max_link_rate = 0x14;
 		video_info->max_lane_count = 0x04;
-		break;
-	case EXYNOS_DP:
-		/*
-		 * NOTE: those property parseing code is used for
-		 * providing backward compatibility for samsung platform.
-		 */
 		of_property_read_u32(dp_node, "samsung,link-rate",
 				     &video_info->max_link_rate);
 		of_property_read_u32(dp_node, "samsung,lane-count",
